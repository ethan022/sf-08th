# C++ 기초 문법

## 📖 개요

C++ 프로그래밍의 기초 문법을 학습하는 프로젝트 모음입니다.

## 📂 프로젝트 목록

### 0402 - C++ 기본 문법
- **주제**: C++ 시작하기
- **학습 내용**:
  - `cout`, `cin` 기본 입출력
  - 변수 선언 및 초기화
  - 기본 데이터 타입 (int, float, double, char, string)
  - 산술 연산자

### 0403 - 함수와 모듈화
- **주제**: 함수 정의 및 헤더 파일
- **학습 내용**:
  - 함수 선언과 정의
  - 헤더 파일 (`func.h`)과 구현 파일 (`func.cpp`) 분리
  - 함수 매개변수와 반환값
  - `#include` 지시문
- **파일**:
  - `0403.cpp` - 메인 프로그램
  - `0403_prac.cpp` - 실습 코드
  - `func.h` - 함수 선언
  - `func.cpp` - 함수 구현

### 0407 - 기초 종합
- **주제**: 기본 문법 종합 실습
- **학습 내용**:
  - 조건문 (if, else, switch)
  - 반복문 (for, while, do-while)
  - 배열 기초

### 0408 - 포인터와 참조자
- **주제**: 메모리와 포인터
- **학습 내용**:
  - 포인터 개념 및 사용법
  - 참조자 (&)
  - 동적 메모리 할당 (`new`, `delete`)
  - 포인터 연산

### ConsoleApplication2 - 초기 학습 프로젝트
- **주제**: 첫 C++ 프로젝트
- **파일**:
  - `0401.cpp` - 4월 1일 실습
  - `0402.cpp` - 4월 2일 실습

## 🎯 핵심 개념

### 1. 기본 입출력
```cpp
#include <iostream>
using namespace std;

int main() {
    int age;
    cout << "나이를 입력하세요: ";
    cin >> age;
    cout << "당신의 나이는 " << age << "세입니다." << endl;
    return 0;
}
```

### 2. 함수
```cpp
// 헤더 파일 (func.h)
#ifndef FUNC_H
#define FUNC_H

int add(int a, int b);

#endif

// 구현 파일 (func.cpp)
#include "func.h"

int add(int a, int b) {
    return a + b;
}
```

### 3. 포인터
```cpp
int num = 10;
int* ptr = &num;        // num의 주소를 ptr에 저장
cout << *ptr;           // 10 출력 (역참조)
*ptr = 20;              // num의 값을 20으로 변경
```

### 4. 참조자
```cpp
int num = 10;
int& ref = num;         // num의 별칭
ref = 20;               // num의 값이 20으로 변경됨
```

## 📚 학습 순서

1. **0402**: C++ 기본 문법 이해
2. **0403**: 함수와 파일 분리 학습
3. **0407**: 제어문 실습
4. **0408**: 포인터 개념 이해

## 💡 주요 학습 포인트

- ✅ C++의 기본 구조 이해
- ✅ 입출력 스트림 사용법
- ✅ 함수 선언/정의 분리의 필요성
- ✅ 포인터와 참조자의 차이
- ✅ 동적 메모리 관리의 중요성

## 🔗 다음 단계

→ **02_data_structures**: STL 컨테이너 학습으로 이동

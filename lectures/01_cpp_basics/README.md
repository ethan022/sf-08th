# C++ 기초 문법

## 📂 프로젝트 목록

### 0402 - C++ 기초 종합
**파일**: `0402.cpp`

**학습 내용**:
- **비트 연산자**: `|`, `&`, `^`, `<<`, `>>` 사용법
- **조건문**: if-else, switch-case
- **반복문**: for, while, do-while
- **배열**: 1차원, 2차원 배열
- **실습 문제**: 별찍기, 구구단, 성적 평균 계산

**주요 코드**:
```cpp
// 비트 연산으로 특정 비트 설정/해제
unsigned char num = 0b00010010;
num = num | (1 << 5);  // 5번 비트 설정

// 홀짝 판별 (비트 연산)
if (number & 1) {
    cout << "홀수";
}

// 성적 계산
int grade[5];
int total = 0;
for (int i = 0; i < 5; i++) {
    cin >> grade[i];
    total += grade[i];
}
double avg = static_cast<double>(total) / 5;
```

---

### 0403 - 함수와 모듈화
**파일**: `0403.cpp`, `func.h`, `func.cpp`

**학습 내용**:
- **함수 선언/정의**: 기본 함수, 반환 함수
- **재귀 함수**: factorial, fibonacci, power
- **정적 변수**: static 키워드
- **전역 변수**: extern 키워드로 다른 파일에서 접근
- **헤더 파일 분리**: 선언부와 구현부 분리

**주요 함수**:
```cpp
// 팩토리얼 (재귀)
int factorial(int n) {
    if (n <= 1) return 1;
    return n * factorial(n - 1);
}

// 피보나치 (재귀)
int fibonacci(int n) {
    if (n <= 0) return 0;
    if (n == 1) return 1;
    return fibonacci(n - 1) + fibonacci(n - 2);
}

// 정적 변수
void func1() {
    static int count = 0;  // 함수 호출 간에 값 유지
    count++;
}
```

**파일 구조**:
- `0403.cpp` - 메인 프로그램
- `func.h` - 함수 선언
- `func.cpp` - 함수 구현

---

### 0407 - 포인터와 동적 메모리
**파일**: `0407.cpp`

**학습 내용**:
- **포인터 기초**: 주소 연산자(`&`), 역참조(`*`)
- **배열과 포인터**: 배열명은 포인터
- **동적 메모리**: `new`, `delete`
- **이중 포인터**: 2차원 배열 동적 할당
- **함수 포인터**: 콜백 함수 구현

**주요 코드**:
```cpp
// 기본 포인터
int num = 42;
int* ptr = &num;
*ptr = 20;  // num의 값 변경

// 동적 배열
int size;
cin >> size;
int* arr = new int[size];
delete[] arr;

// 2차원 동적 배열
int** matrix = new int*[rows];
for (int i = 0; i < rows; i++) {
    matrix[i] = new int[cols];
}
// ... 사용 후 해제
for (int i = 0; i < rows; i++) {
    delete[] matrix[i];
}
delete[] matrix;

// 함수 포인터
void (*funcPtr)() = hello;
funcPtr();  // hello 함수 호출

int (*addPtr)(int, int) = add;
cout << addPtr(10, 20);  // add(10, 20) 호출
```

---

### 0408 - (내용 확인 필요)
**파일**: `0408.cpp`

---

### ConsoleApplication2 - 초기 학습
**파일**: `0401.cpp`, `0402.cpp`

최초 C++ 학습 프로젝트

---

## 🎯 학습 흐름

```
0402 (기본 문법) → 0403 (함수) → 0407 (포인터) → 0408 (종합)
```

## 💡 핵심 개념

### 1. 비트 연산자
- `|` (OR) - 비트 설정
- `&` (AND) - 비트 확인/해제
- `^` (XOR) - 비트 토글
- `<<` (왼쪽 시프트) - 2배씩 증가
- `>>` (오른쪽 시프트) - 2배씩 감소

### 2. 함수
- **선언과 정의 분리**: 헤더 파일 활용
- **재귀 함수**: 기본 조건(base case) 필수
- **정적 변수**: 함수 호출 간 값 유지

### 3. 포인터
- **기본 개념**: 메모리 주소를 저장
- **동적 메모리**: 런타임에 크기 결정
- **메모리 해제**: `delete`로 누수 방지
- **이중 포인터**: 포인터의 포인터

### 4. 배열
- **정적 배열**: 컴파일 타임에 크기 결정
- **동적 배열**: `new[]`로 할당
- **2차원 배열**: 행렬 구현

## 📝 실습 과제

각 파일의 주석 처리된 부분을 활성화하여 다양한 예제를 실행해보세요.

### 추천 실습 순서
1. 0402 - 기본 문법 익히기
2. 0403 - 함수 만들어보기
3. 0407 - 포인터 이해하기
4. 자신만의 프로그램 만들기

## ⚠️ 주의사항

1. **메모리 관리**: `new`로 할당한 메모리는 반드시 `delete`로 해제
2. **정적 변수**: 함수 내 static 변수는 초기화 한 번만 수행
3. **재귀 함수**: 기본 조건 없으면 무한 재귀
4. **포인터**: 초기화되지 않은 포인터 사용 금지

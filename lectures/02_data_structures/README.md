# STL 자료구조

## 📂 프로젝트 목록

### 0408_vector - std::vector 학습
**파일**: `0408_vector.cpp`

**학습 내용**:
- `vector` 선언 및 초기화
- 요소 접근: `at()`, `[]`, `front()`, `back()`
- 요소 추가/삭제: `push_back()`, `pop_back()`, `insert()`, `erase()`
- 반복자(iterator) 사용
- 벡터 정렬: `sort()`

**주요 코드**:
```cpp
// 벡터 선언
vector<int> v1;                    // 빈 벡터
vector<int> v2(5, 10);             // 크기 5, 값 10으로 초기화
vector<int> v3 = {1, 2, 3, 4, 5};  // 초기화 리스트

// 요소 접근
v3.at(2);      // 안전한 접근 (범위 검사)
v3[2];         // 빠른 접근 (범위 검사 X)
v3.front();    // 첫 요소
v3.back();     // 마지막 요소

// 요소 추가/삭제
v3.push_back(6);               // 끝에 추가
v3.pop_back();                 // 끝에서 제거
v3.insert(v3.begin() + 3, 3);  // 3번 인덱스에 3 삽입
v3.erase(v3.begin() + 2);      // 2번 인덱스 제거

// 정렬
sort(v3.begin(), v3.end());    // 오름차순
```

---

### 0408_list - std::list 학습
**파일**: `0408_list.cpp`

**학습 내용**:
- `list` 선언 및 초기화
- 양쪽 끝 추가/삭제: `push_front()`, `push_back()`, `pop_front()`, `pop_back()`
- 리스트 정렬: `sort()`
- 리스트 역순: `reverse()`
- 리스트 크기: `size()`

**주요 코드**:
```cpp
// 리스트 선언
list<int> l1;
list<int> l2 = {1, 2, 3, 4, 5};

// 양쪽 끝에 추가
l2.push_front(0);    // 앞에 추가
l2.push_back(6);     // 뒤에 추가

// 양쪽 끝에서 제거
l2.pop_front();      // 앞에서 제거
l2.pop_back();       // 뒤에서 제거

// 정렬 및 역순
l2.sort();           // 오름차순 정렬
l2.reverse();        // 역순

// 반복자로 순회
for (auto it = l2.begin(); it != l2.end(); ++it) {
    cout << *it << " ";
}
```

---

## 🎯 Vector vs List 비교

| 특징 | vector | list |
|------|--------|------|
| **구조** | 동적 배열 | 이중 연결 리스트 |
| **메모리** | 연속적 | 비연속적 |
| **임의 접근** | O(1) ✅ | O(n) ❌ |
| **중간 삽입/삭제** | O(n) | O(1) ✅ |
| **끝 삽입/삭제** | O(1) | O(1) |
| **메모리 효율** | 높음 | 낮음 (포인터 오버헤드) |

## 💡 언제 사용할까?

### Vector 사용 시기
- 인덱스로 자주 접근할 때
- 끝에서만 추가/삭제할 때
- 메모리 효율이 중요할 때
- 데이터를 순차적으로 읽을 때

### List 사용 시기
- 중간에 삽입/삭제가 빈번할 때
- 양쪽 끝에서 추가/삭제할 때
- 임의 접근이 필요 없을 때
- 큐(Queue)나 스택(Stack) 구현할 때

## 📝 핵심 개념

### 1. Iterator (반복자)
컨테이너의 요소를 순회하는 포인터와 유사한 객체
```cpp
vector<int>::iterator it = v.begin();
while (it != v.end()) {
    cout << *it << " ";
    ++it;
}
```

### 2. 범위 기반 for문
```cpp
for (int num : v) {
    cout << num << " ";
}
```

### 3. at() vs []
- `at()`: 범위 검사 O, 예외 발생
- `[]`: 범위 검사 X, 빠름, 위험

## 🚀 실습 과제

1. **Vector로 학생 성적 관리 시스템 만들기**
   - 성적 입력, 평균 계산, 정렬

2. **List로 작업 큐(Task Queue) 구현**
   - 작업 추가, 작업 처리, 우선순위 작업 앞에 삽입

3. **두 컨테이너 성능 비교**
   - 10000개 요소 중간 삽입 시간 측정
